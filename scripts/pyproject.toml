[project]
name = "scripts"
version = "0.1.0"
description = "UPI and USSD payment automation helpers using ADB, scrcpy, and uiautomator2."
readme = "README.md"
requires-python = ">=3.12"
keywords = ["upi", "ussd", "automation", "adb", "scrcpy", "uiautomator2"]
dependencies = []
